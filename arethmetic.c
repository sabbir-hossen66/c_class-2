#include <stdio.h>
int main()
{
    int sabbir;
    float sakib;
    char sam;
    scanf("%d %f %c", &sabbir, &sakib, &sam);
    printf("%d,%0.1f,%c", sabbir, sakib, sam);
    // printf("bangladesh");
    return 0;
}